<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/register_login_background"
    tools:context=".LoginSayfasi">


    <EditText
        android:layout_marginTop="150dp"
        android:padding="@dimen/activity_vertical_margin"
        android:id="@+id/email_edittext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Email"
        android:background="@color/beyaz"
        android:inputType="textPersonName" />

    <EditText
        android:padding="@dimen/activity_vertical_margin"
        android:layout_marginTop="@dimen/nav_header_vertical_spacing"
        android:id="@+id/parola_edittext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Parola"
        android:background="@color/beyaz"
        android:inputType="textPassword" />

    <Button
        android:onClick="anasayfayaGit"
        android:padding="@dimen/activity_vertical_margin"
        android:layout_marginTop="@dimen/nav_header_vertical_spacing"
        android:id="@+id/giris_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:textColor="@color/beyaz"
        android:text="Giriş" />

    <Button
        android:onClick="kayitSayfasinaGit"
        android:padding="@dimen/activity_vertical_margin"
        android:layout_marginTop="@dimen/nav_header_vertical_spacing"
        android:id="@+id/kayit_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:textColor="@color/beyaz"
        android:text="Kayıt OL" />
</LinearLayout>